/**
 * Global stores barrel export.
 * Feature-specific stores live in their respective feature directories
 * (e.g., features/auth/store/auth.store.ts) and are re-exported here
 * for convenience.
 */
export { useAuthStore } from '../features/auth/store/auth.store';
